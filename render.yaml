services:
  - type: web
    name: aquamaster
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn aquamaster_api:app --workers 1 --threads 2 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
